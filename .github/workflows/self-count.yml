name: Self Count

on:
  push:
    tags:
      - 'v*'

jobs:
  test-self:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout this repo
        uses: actions/checkout@v4

      - name: Run LOC count on self
        uses: ./
        with:
          publish: ${{ secrets.GITHUB_TOKEN }}
